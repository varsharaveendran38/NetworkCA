version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/app

hooks:
  BeforeInstall:
    - location: scripts/stop_docker.sh
      timeout: 300
      runas: ubuntu

  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: ubuntu
